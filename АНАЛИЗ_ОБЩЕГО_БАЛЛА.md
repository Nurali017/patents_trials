# Анализ формирования общего балла сорта

## 📊 Общая структура

Общий балл сорта рассчитывается в классе `BallScorer` (файл `trials_app/evaluation/ball_scorer.py`) методом `calculate_overall_score()`.

### Схема процесса расчета:

```
┌─────────────────────────────────────────────────────────────┐
│                    ВХОДНЫЕ ДАННЫЕ                            │
│  sort_data: { regions_data, summary, ... }                  │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  calculate_overall_score()   │
        └──────────────┬───────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  Урожайность │ │   Качество   │ │ Устойчивость │
│   (40%)      │ │    (30%)     │ │    (30%)     │
│              │ │              │ │              │
│ _calculate_  │ │ QualityEval. │ │ Resistance   │
│ yield_score()│ │ calculate_   │ │ Checker      │
│              │ │ quality_     │ │ calculate_   │
│              │ │ score()      │ │ resistance_  │
│              │ │              │ │ score()      │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
       │                │                │
       └────────┬───────┴────────────────┘
                │
                ▼
        ┌───────────────────────┐
        │  Взвешенная сумма:    │
        │  Σ(балл × вес) /      │
        │  Σ(доступные веса)    │
        └───────────┬───────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │   ОБЩИЙ БАЛЛ (1-5)    │
        │   + интерпретация     │
        └───────────────────────┘
```

## 📐 Формула расчета

Общий балл формируется как **взвешенная сумма** трех компонентов:

### Базовая формула (когда все компоненты доступны):

```
Общий балл = Балл_урожайности × 0.4 + Балл_качества × 0.3 + Балл_устойчивости × 0.3
```

### Адаптивная формула (когда некоторые компоненты отсутствуют):

```
Общий балл = Σ(доступные_баллы × их_веса) / Σ(доступные_веса)
```

**Пример адаптивного расчета:**
- Если качество = None, то: `(Урожайность × 0.4 + Устойчивость × 0.3) / (0.4 + 0.3)`

### Веса компонентов (по Методике ГСИ):
- **Урожайность**: 40% (0.4)
- **Качество**: 30% (0.3)
- **Устойчивость**: 30% (0.3)

### Особенности расчета:
1. Если какой-то компонент отсутствует (None), его вес не учитывается
2. Общий балл рассчитывается только по доступным данным
3. Если нет ни одного компонента, используется балл урожайности или значение по умолчанию (3.0)

---

## Компонент 1: Балл по урожайности (40%)

**Метод**: `_calculate_yield_score()` в `BallScorer`

### Алгоритм расчета:

1. **Сбор данных**: Анализируются все регионы испытаний (`regions_data`)
2. **Расчет среднего отклонения**:
   ```
   avg_deviation = Σ(deviation_from_standard) / количество_регионов
   ```
3. **Расчет процента статистически значимых регионов**:
   ```
   significance_percent = (количество_значимых_регионов / общее_количество) × 100%
   ```

### Логика определения балла (1-5):

#### Если сорт превышает стандарт (avg_deviation > 0):
- **5 баллов**: `significance_percent >= 75%` → "Значительно превышает стандарт (статистически значимо)"
- **4 балла**: 
  - `significance_percent >= 50%` → "Статистически значимо превышает стандарт"
  - ИЛИ `avg_deviation >= 5.0%` → "Значительно превышает стандарт (требует подтверждения)"
- **3 балла**: 
  - `avg_deviation >= 2.0%` → "Превышает стандарт (требует подтверждения)"
  - ИЛИ `avg_deviation < 2.0%` → "Незначительно превышает стандарт"

#### Если сорт уступает стандарту (avg_deviation < 0):
- **2 балла**: 
  - `significance_percent >= 50%` → "Статистически значимо уступает стандарту"
  - ИЛИ `abs(avg_deviation) >= 5.0%` → "Значительно уступает стандарту"
  - ИЛИ `abs(avg_deviation) >= 2.0%` → "Уступает стандарту"
- **3 балла**: `abs(avg_deviation) < 2.0%` → "Незначительно уступает стандарту"

#### Если равен стандарту (avg_deviation ≈ 0):
- **3 балла**: "Не отличается от стандарта"

---

## Компонент 2: Балл по качеству (30%)

**Класс**: `QualityEvaluator` (файл `trials_app/evaluation/quality_evaluator.py`)

### Оцениваемые показатели:

1. **Содержание белка** (`protein_content`)
   - Отлично (5): ≥ 15.0%
   - Хорошо (4): ≥ 13.0%
   - Удовлетворительно (3): ≥ 11.0%
   - Посредственно (2): ≥ 9.0%
   - Плохо (1): < 9.0%

2. **Содержание клейковины** (`gluten_content`)
   - Отлично (5): ≥ 30.0%
   - Хорошо (4): ≥ 25.0%
   - Удовлетворительно (3): ≥ 20.0%
   - Посредственно (2): ≥ 15.0%
   - Плохо (1): < 15.0%

3. **Стекловидность** (`vitreousness`)
   - Отлично (5): ≥ 90.0%
   - Хорошо (4): ≥ 80.0%
   - Удовлетворительно (3): ≥ 70.0%
   - Посредственно (2): ≥ 60.0%
   - Плохо (1): < 60.0%

4. **Масса 1000 зерен** (`thousand_seed_weight`)
   - Отлично (5): ≥ 50.0 г
   - Хорошо (4): ≥ 45.0 г
   - Удовлетворительно (3): ≥ 40.0 г
   - Посредственно (2): ≥ 35.0 г
   - Плохо (1): < 35.0 г

### Алгоритм расчета:

1. **Извлечение показателей** из `regions_data` и `summary`
2. **Оценка каждого показателя** по шкале 1-5
3. **Средний балл**:
   ```
   avg_score = Σ(баллы_показателей) / количество_показателей
   ```
4. **Если данных нет**: возвращается `None` (компонент не учитывается в общем балле)

---

## Компонент 3: Балл по устойчивости (30%)

**Класс**: `ResistanceChecker` (файл `trials_app/evaluation/resistance_checker.py`)

### Оцениваемые показатели (шкала 1-10):

1. **Устойчивость к болезням** (`disease_resistance`)
   - Отлично (5): ≥ 9.0
   - Хорошо (4): ≥ 7.0
   - Удовлетворительно (3): ≥ 5.0
   - Посредственно (2): ≥ 3.0
   - Плохо (1): < 3.0

2. **Устойчивость к полеганию** (`lodging_resistance`)
   - Отлично (5): ≥ 7.0
   - Хорошо (4): ≥ 5.0
   - Удовлетворительно (3): ≥ 3.0
   - Посредственно (2): ≥ 1.0
   - Плохо (1): < 1.0

3. **Засухоустойчивость** (`drought_resistance`)
   - Отлично (5): ≥ 7.0
   - Хорошо (4): ≥ 5.0
   - Удовлетворительно (3): ≥ 3.0
   - Посредственно (2): ≥ 1.0
   - Плохо (1): < 1.0

4. **Зимостойкость** (`winter_hardiness`)
   - Отлично (5): ≥ 7.0
   - Хорошо (4): ≥ 5.0
   - Удовлетворительно (3): ≥ 3.0
   - Посредственно (2): ≥ 1.0
   - Плохо (1): < 1.0

5. **Устойчивость к осыпанию** (`shattering_resistance`)
   - Отлично (5): ≥ 7.0
   - Хорошо (4): ≥ 5.0
   - Удовлетворительно (3): ≥ 3.0
   - Посредственно (2): ≥ 1.0
   - Плохо (1): < 1.0

6. **Устойчивость к прорастанию** (`sprouting_resistance`)
   - Отлично (5): ≥ 7.0
   - Хорошо (4): ≥ 5.0
   - Удовлетворительно (3): ≥ 3.0
   - Посредственно (2): ≥ 1.0
   - Плохо (1): < 1.0

### Алгоритм расчета:

1. **Извлечение показателей** из всех регионов
2. **Усреднение значений** по каждому показателю (если есть данные из нескольких регионов)
3. **Оценка каждого показателя** по шкале 1-5
4. **Средний балл**:
   ```
   avg_score = Σ(баллы_показателей) / количество_показателей
   ```
5. **Если данных нет**: возвращается `None` (компонент не учитывается в общем балле)

---

## Финальный расчет общего балла

### Код расчета (строки 40-63 в `ball_scorer.py`):

```python
# Общий балл с весами (только для доступных данных)
total_weight = 0
weighted_sum = 0

# Урожайность (всегда есть)
if yield_score['score'] is not None:
    weighted_sum += yield_score['score'] * self.weights['yield']  # × 0.4
    total_weight += self.weights['yield']  # 0.4

# Качество
if quality_score['score'] is not None:
    weighted_sum += quality_score['score'] * self.weights['quality']  # × 0.3
    total_weight += self.weights['quality']  # 0.3

# Устойчивость
if resistance_score['score'] is not None:
    weighted_sum += resistance_score['score'] * self.weights['resistance']  # × 0.3
    total_weight += self.weights['resistance']  # 0.3

# Рассчитываем общий балл только по доступным данным
if total_weight > 0:
    overall_score = weighted_sum / total_weight
else:
    overall_score = yield_score['score'] if yield_score['score'] is not None else 3
```

### Примеры расчета:

#### Пример 1: Все компоненты доступны
- Урожайность: 4.0 балла
- Качество: 3.5 балла
- Устойчивость: 4.0 балла

```
weighted_sum = 4.0 × 0.4 + 3.5 × 0.3 + 4.0 × 0.3 = 1.6 + 1.05 + 1.2 = 3.85
total_weight = 0.4 + 0.3 + 0.3 = 1.0
overall_score = 3.85 / 1.0 = 3.85 ≈ 3.9
```

#### Пример 2: Отсутствуют данные по качеству
- Урожайность: 4.0 балла
- Качество: None (нет данных)
- Устойчивость: 3.5 балла

```
weighted_sum = 4.0 × 0.4 + 3.5 × 0.3 = 1.6 + 1.05 = 2.65
total_weight = 0.4 + 0.3 = 0.7
overall_score = 2.65 / 0.7 = 3.79 ≈ 3.8
```

#### Пример 3: Только урожайность
- Урожайность: 4.0 балла
- Качество: None
- Устойчивость: None

```
overall_score = 4.0 (используется балл урожайности напрямую)
```

#### Пример 4: Реальный расчет с деталями

**Исходные данные:**
- Среднее отклонение от стандарта: +3.5%
- Статистически значимых регионов: 60%
- Качество: белок 14.2%, клейковина 28.5%
- Устойчивость: к болезням 7.5, к полеганию 6.0

**Шаг 1: Балл урожайности**
- `avg_deviation = +3.5%` (превышает стандарт)
- `significance_percent = 60%` (≥ 50%)
- **Результат: 4 балла** ("Статистически значимо превышает стандарт")

**Шаг 2: Балл качества**
- Белок 14.2% → 4 балла (≥ 13.0%)
- Клейковина 28.5% → 4 балла (≥ 25.0%)
- Средний: (4 + 4) / 2 = **4.0 балла**

**Шаг 3: Балл устойчивости**
- К болезням 7.5 → 4 балла (≥ 7.0)
- К полеганию 6.0 → 4 балла (≥ 5.0)
- Средний: (4 + 4) / 2 = **4.0 балла**

**Шаг 4: Общий балл**
```
weighted_sum = 4.0 × 0.4 + 4.0 × 0.3 + 4.0 × 0.3
             = 1.6 + 1.2 + 1.2 = 4.0
total_weight = 0.4 + 0.3 + 0.3 = 1.0
overall_score = 4.0 / 1.0 = 4.0
```

**Итог:** Общий балл = **4.0** → "Хороший, не уступает большинству сортов"

---

## Интерпретация общего балла

**Метод**: `_get_interpretation()` в `BallScorer`

- **≥ 4.5**: "Отличный сорт"
- **≥ 3.5**: "Хороший, не уступает большинству сортов"
- **≥ 2.5**: "Удовлетворительный, требует дополнительного изучения"
- **≥ 1.5**: "Посредственный, возможный кандидат к снятию"
- **< 1.5**: "Плохой, рекомендуется к снятию"

---

## Использование общего балла в системе

### В годовых отчетах (`summary_service.py`):

1. **Расчет балла** (строка 106):
   ```python
   evaluation_scores = self.ball_scorer.calculate_overall_score(sort_data)
   ```

2. **Критерии для рекомендаций**:
   - **Одобрение** (`approved`): `overall_score >= 4.0`
   - **Отклонение** (`rejected`): `overall_score <= 2.0`
   - **Продолжение испытаний** (`continue`): `2.0 < overall_score < 4.0`

3. **Дополнительные проверки**:
   - Балл урожайности должен быть ≥ 4
   - Балл качества должен быть ≥ 3
   - Балл устойчивости должен быть ≥ 3

---

## Возвращаемая структура данных

Метод `calculate_overall_score()` возвращает словарь:

```python
{
    'yield_score': float | None,           # Балл по урожайности (1-5)
    'quality_score': float | None,         # Балл по качеству (1-5)
    'resistance_score': float | None,      # Балл по устойчивости (1-5)
    'overall_score': float,                # Общий балл (1-5, округлен до 0.1)
    'score_interpretation': str,           # Текстовая интерпретация
    'detailed_scores': {
        'yield': {
            'score': float,
            'interpretation': str,
            'deviation_from_standard': float,
            'statistical_significance': bool,
            'significant_regions_percent': float
        },
        'quality': {
            'score': float,
            'interpretation': str,
            'indicators': {...},
            'sufficient_data': bool
        },
        'resistance': {
            'score': float,
            'interpretation': str,
            'indicators': {...},
            'sufficient_data': bool
        }
    }
}
```

---

## ⚠️ Важные замечания

1. **Округление**: Общий балл округляется до 1 знака после запятой (метод `round(overall_score, 1)`)
2. **Обработка None**: Если компонент отсутствует, он не учитывается в расчете, но вес не перераспределяется автоматически - используется адаптивная нормализация
3. **Минимальные требования**: 
   - Для качества и устойчивости требуется минимум 2 показателя для достаточности данных (`sufficient_data`)
   - Если данных недостаточно, компонент возвращает `None` и не участвует в расчете
4. **Статистическая значимость**: При расчете балла урожайности учитывается процент регионов со статистически значимыми результатами (порог 50% для значимости)
5. **Приоритет данных**: 
   - Урожайность всегда присутствует (минимальное значение 3.0, если данных нет)
   - Качество и устойчивость могут отсутствовать полностью
6. **Fallback логика**: Если `total_weight = 0` (все компоненты None), используется балл урожайности или значение по умолчанию 3.0

---

## 📁 Файлы, связанные с расчетом

- `trials_app/evaluation/ball_scorer.py` - основной класс расчета общего балла
  - Метод `calculate_overall_score()` - главный метод расчета
  - Метод `_calculate_yield_score()` - расчет балла урожайности
  - Метод `_get_interpretation()` - интерпретация общего балла
  - Метод `get_decision_criteria()` - критерии для принятия решений

- `trials_app/evaluation/quality_evaluator.py` - расчет балла по качеству
  - Метод `calculate_quality_score()` - расчет балла качества
  - Оценка 4 показателей: белок, клейковина, стекловидность, масса 1000 зерен

- `trials_app/evaluation/resistance_checker.py` - расчет балла по устойчивости
  - Метод `calculate_resistance_score()` - расчет балла устойчивости
  - Оценка 6 показателей: болезни, полегание, засуха, зимостойкость, осыпание, прорастание

- `trials_app/views/services/summary_service.py` - использование баллов в годовых отчетах
  - Интеграция с `BallScorer` для генерации годовых отчетов
  - Использование баллов для принятия решений (approved/rejected/continue)

---

## 🔍 Детальный разбор алгоритма расчета урожайности

### Дерево решений для балла урожайности:

```
avg_deviation > 0? (превышает стандарт)
│
├─ ДА → significance_percent >= 75%?
│        │
│        ├─ ДА → 5 баллов
│        │
│        └─ НЕТ → significance_percent >= 50%?
│                 │
│                 ├─ ДА → 4 балла
│                 │
│                 └─ НЕТ → avg_deviation >= 5.0%?
│                          │
│                          ├─ ДА → 4 балла
│                          │
│                          └─ НЕТ → avg_deviation >= 2.0%?
│                                   │
│                                   ├─ ДА → 3 балла
│                                   │
│                                   └─ НЕТ → 3 балла
│
└─ НЕТ → avg_deviation < 0? (уступает стандарту)
         │
         ├─ ДА → significance_percent >= 50%?
         │        │
         │        ├─ ДА → 2 балла
         │        │
         │        └─ НЕТ → abs(avg_deviation) >= 5.0%?
         │                 │
         │                 ├─ ДА → 2 балла
         │                 │
         │                 └─ НЕТ → abs(avg_deviation) >= 2.0%?
         │                          │
         │                          ├─ ДА → 2 балла
         │                          │
         │                          └─ НЕТ → 3 балла
         │
         └─ НЕТ → 3 балла (равен стандарту)
```

---

## 📈 Критерии принятия решений на основе общего балла

### В методе `get_decision_criteria()`:

**Критерии одобрения (`approved_criteria`):**
- `overall_score >= 4.0` - общий балл высокий
- `yield_score >= 4` - урожайность хорошая
- `quality_score >= 3` - качество удовлетворительное
- `resistance_score >= 3` - устойчивость удовлетворительная
- `all_criteria_met` - все критерии выполнены

**Критерии отклонения (`rejected_criteria`):**
- `overall_score <= 2.0` - общий балл критически низкий
- `yield_score <= 2` - урожайность низкая
- `quality_score <= 2` - качество низкое
- `resistance_score <= 2` - устойчивость низкая
- `any_criteria_failed` - хотя бы один критерий не выполнен

**Рекомендации:**
- `approved` - если все критерии одобрения выполнены
- `rejected` - если хотя бы один критерий отклонения выполнен
- `continue` - во всех остальных случаях (требуется продолжение испытаний)

---

## 🎯 Полная логика формирования рекомендаций

**Метод**: `_make_recommendation_new()` в `SummaryService` (файл `trials_app/views/services/summary_service.py`)

Этот метод использует **guard clauses** (защитные условия) для последовательной проверки критериев и формирования финальной рекомендации.

### Схема процесса принятия решения:

```
┌─────────────────────────────────────────────────────────────┐
│              ВХОДНЫЕ ДАННЫЕ                                  │
│  overall_summary: { coverage_percent, min_years_tested }    │
│  evaluation_scores: { overall_score, yield_score, ... }     │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  GUARD CLAUSE 1: Hard Reject │
        │  (Жесткое отклонение)        │
        └──────────────┬───────────────┘
                       │
        ┌──────────────┴──────────────┐
        │  Проверка критических       │
        │  условий отклонения         │
        └──────────────┬──────────────┘
                       │
        ┌──────────────┴──────────────┐
        │  Есть причины для reject?   │
        └──────────────┬──────────────┘
                       │
        ┌──────────────┴──────────────┐
        │  ДА → return 'rejected'     │
        │  НЕТ → продолжить           │
        └──────────────┬──────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  GUARD CLAUSE 2: Approval    │
        │  (Одобрение)                 │
        └──────────────┬───────────────┘
                       │
        ┌──────────────┴──────────────┐
        │  Проверка всех условий      │
        │  для одобрения              │
        └──────────────┬──────────────┘
                       │
        ┌──────────────┴──────────────┐
        │  Все условия выполнены?     │
        └──────────────┬──────────────┘
                       │
        ┌──────────────┴──────────────┐
        │  ДА → return 'approved'     │
        │  НЕТ → продолжить           │
        └──────────────┬──────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  DEFAULT: Continue           │
        │  (Продолжить испытания)      │
        └──────────────┬───────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  return 'continue'           │
        │  + список причин             │
        └──────────────────────────────┘
```

---

### 🔴 GUARD CLAUSE 1: Hard Reject (Жесткое отклонение)

**Приоритет**: Высший (проверяется первым)

**Условия для отклонения** (достаточно одного):

1. **Критически низкое покрытие ГСУ**:
   ```
   coverage_percent < 20%
   ```
   - **Причина**: `"Критически низкое покрытие ГСУ ({coverage_percent}%)"`

2. **Отсутствие данных испытаний**:
   ```
   min_years_tested < 1
   ```
   - **Причина**: `"Отсутствуют данные испытаний (лет: {min_years_tested})"`

3. **Критически низкий общий балл**:
   ```
   overall_score <= 2.0
   ```
   - **Причина**: `"Критически низкий общий балл ({overall_score}/5)"`

4. **Критически низкая урожайность**:
   ```
   yield_score is not None AND yield_score <= 2
   ```
   - **Причина**: `"Критически низкая урожайность (балл {yield_score}/5)"`

**Возвращаемая структура при отклонении:**

```python
{
    'decision': 'rejected',
    'reason': 'Сорт отклонен. {список причин}.',
    'confidence': 'high',
    'can_approve': False,
    'justification': {
        'coverage_criteria': {
            'met': bool,  # coverage_percent >= 20
            'description': str
        },
        'years_tested_criteria': {
            'met': bool,  # min_years_tested >= 1
            'description': str
        },
        'overall_score_criteria': {
            'met': bool,  # overall_score > 2.0
            'description': str
        },
        'yield_criteria': {
            'met': bool,  # yield_score is None or yield_score > 2
            'description': str
        }
    },
    'improvement_recommendations': [
        'Расширить географию испытаний для увеличения покрытия ГСУ',
        'Провести дополнительные испытания минимум в течение 2 лет',
        'Улучшить показатели урожайности и качества'
    ]
}
```

**Пример отклонения:**

```python
# Входные данные:
coverage_percent = 15.0  # < 20%
min_years_tested = 1
overall_score = 2.5
yield_score = 3.0

# Результат:
{
    'decision': 'rejected',
    'reason': 'Сорт отклонен. Критически низкое покрытие ГСУ (15.0%).',
    'confidence': 'high',
    'can_approve': False
}
```

---

### 🟢 GUARD CLAUSE 2: Approval (Одобрение)

**Приоритет**: Второй (проверяется после отклонения)

**Условия для одобрения** (все должны быть выполнены одновременно):

```python
can_approve = (
    coverage_percent >= 50 and           # Покрытие ГСУ ≥ 50%
    min_years_tested >= 2 and            # Минимум 2 года испытаний
    overall_score >= 4.0 and             # Общий балл ≥ 4.0
    yield_score is not None and          # Данные урожайности есть
    yield_score >= 4 and                 # Балл урожайности ≥ 4
    (quality_score is not None and       # Данные качества есть
     quality_score >= 3) and             # Балл качества ≥ 3
    (resistance_score is None or         # Устойчивость может отсутствовать
     resistance_score >= 3)              # Если есть, то ≥ 3
)
```

**Критическое замечание**: 
- `quality_score` **обязательно должен быть не None** и ≥ 3
- `resistance_score` может быть `None` (необязательный параметр)

**Возвращаемая структура при одобрении:**

```python
{
    'decision': 'approved',
    'reason': 'Сорт рекомендован к утверждению. Балльная оценка: {overall_score}/5. Покрытие ГСУ: {coverage_percent}%. Испытания проведены {min_years_tested}+ лет.',
    'confidence': 'high',
    'can_approve': True,
    'justification': {
        'coverage_criteria': {
            'met': bool,  # coverage_percent >= 50
            'description': str
        },
        'years_tested_criteria': {
            'met': bool,  # min_years_tested >= 2
            'description': str
        },
        'overall_score_criteria': {
            'met': bool,  # overall_score >= 4.0
            'description': str
        },
        'yield_criteria': {
            'met': bool,  # yield_score >= 4
            'description': str
        },
        'quality_criteria': {
            'met': bool,  # quality_score >= 3
            'description': str
        },
        'resistance_criteria': {
            'met': bool,  # resistance_score is None or resistance_score >= 3
            'description': str
        }
    },
    'improvement_recommendations': []  # Пустой список для одобренных
}
```

**Пример одобрения:**

```python
# Входные данные:
coverage_percent = 65.0  # ≥ 50%
min_years_tested = 3     # ≥ 2
overall_score = 4.2      # ≥ 4.0
yield_score = 4.5        # ≥ 4
quality_score = 3.5      # ≥ 3 (и не None!)
resistance_score = 4.0   # ≥ 3

# Результат:
{
    'decision': 'approved',
    'reason': 'Сорт рекомендован к утверждению. Балльная оценка: 4.2/5. Покрытие ГСУ: 65.0%. Испытания проведены 3+ лет.',
    'confidence': 'high',
    'can_approve': True
}
```

**Пример НЕ одобрения (из-за отсутствия качества):**

```python
# Входные данные:
coverage_percent = 60.0
min_years_tested = 2
overall_score = 4.5
yield_score = 4.5
quality_score = None  # ❌ КРИТИЧНО: None не проходит!
resistance_score = 4.0

# Результат: НЕ одобрен, переходит к 'continue'
```

---

### 🟡 DEFAULT: Continue (Продолжить испытания)

**Приоритет**: Низший (используется, если не сработали предыдущие guard clauses)

**Условие**: Все случаи, которые не прошли проверки на отклонение и одобрение

**Формирование списка причин для продолжения:**

1. **Недостаточное покрытие ГСУ**:
   ```
   if coverage_percent < 50:
       reasons_continue.append('Недостаточное покрытие ГСУ (текущее: {coverage_percent}%, требуется: ≥50%)')
   ```

2. **Недостаточная продолжительность испытаний**:
   ```
   if min_years_tested < 2:
       reasons_continue.append('Недостаточная продолжительность испытаний (текущее: {min_years_tested} лет, требуется: ≥2)')
   ```

3. **Общий балл ниже требуемого**:
   ```
   if overall_score < 4.0:
       reasons_continue.append('Общий балл ниже требуемого (текущий: {overall_score}/5, требуется: ≥4.0)')
   ```

4. **Урожайность не соответствует требованиям**:
   ```
   if yield_score is None or yield_score < 4:
       reasons_continue.append('Урожайность не соответствует требованиям (данные: {yield_score}, требуется: ≥4)')
   ```

5. **Качество не соответствует требованиям**:
   ```
   if quality_score is None:
       reasons_continue.append('Отсутствуют данные по качеству (требуется: балл ≥3)')
   elif quality_score < 3:
       reasons_continue.append('Качество не соответствует требованиям (текущее: {quality_score}/5, требуется: ≥3)')
   ```

**Возвращаемая структура при продолжении:**

```python
{
    'decision': 'continue',
    'reason': 'Требуется продолжение испытаний. Балльная оценка: {overall_score}/5. {первые 2 причины}.',
    'confidence': 'medium',
    'can_approve': False,
    'justification': {
        'coverage_criteria': {...},
        'years_tested_criteria': {...},
        'overall_score_criteria': {...},
        'yield_criteria': {...},
        'quality_criteria': {...},
        'resistance_criteria': {...}
    },
    'reasons_to_continue': [
        # Полный список всех причин
    ],
    'improvement_recommendations': [
        'Продолжить испытания для накопления данных',
        'Расширить географию испытаний для увеличения покрытия ГСУ',
        'Обратить внимание на повышение показателей качества и урожайности'
    ]
}
```

**Пример продолжения:**

```python
# Входные данные:
coverage_percent = 35.0  # < 50%
min_years_tested = 1     # < 2
overall_score = 3.5      # < 4.0
yield_score = 3.5        # < 4
quality_score = 2.8      # < 3
resistance_score = 3.5

# Результат:
{
    'decision': 'continue',
    'reason': 'Требуется продолжение испытаний. Балльная оценка: 3.5/5. Недостаточное покрытие ГСУ (текущее: 35.0%, требуется: ≥50%); Недостаточная продолжительность испытаний (текущее: 1 лет, требуется: ≥2).',
    'confidence': 'medium',
    'can_approve': False,
    'reasons_to_continue': [
        'Недостаточное покрытие ГСУ (текущее: 35.0%, требуется: ≥50%)',
        'Недостаточная продолжительность испытаний (текущее: 1 лет, требуется: ≥2)',
        'Общий балл ниже требуемого (текущий: 3.5/5, требуется: ≥4.0)',
        'Урожайность не соответствует требованиям (данные: 3.5/5, требуется: ≥4)',
        'Качество не соответствует требованиям (текущее: 2.8/5, требуется: ≥3)'
    ]
}
```

---

### 📊 Сводная таблица критериев

| Критерий | Rejected | Approved | Continue |
|----------|----------|----------|----------|
| **Покрытие ГСУ** | < 20% | ≥ 50% | 20% ≤ x < 50% |
| **Лет испытаний** | < 1 | ≥ 2 | 1 ≤ x < 2 |
| **Общий балл** | ≤ 2.0 | ≥ 4.0 | 2.0 < x < 4.0 |
| **Балл урожайности** | ≤ 2 | ≥ 4 | 2 < x < 4 |
| **Балл качества** | ≤ 2 | ≥ 3 (обязательно) | < 3 или None |
| **Балл устойчивости** | ≤ 2 | ≥ 3 (или None) | < 3 (если есть) |

**Примечания:**
- Для `rejected`: достаточно **одного** критерия
- Для `approved`: **все** критерии должны быть выполнены
- Для `continue`: все остальные случаи

---

### 🔄 Последовательность проверок (приоритет)

```
1. Проверка на REJECTED (высший приоритет)
   ├─ coverage_percent < 20%? → REJECTED
   ├─ min_years_tested < 1? → REJECTED
   ├─ overall_score <= 2.0? → REJECTED
   └─ yield_score <= 2? → REJECTED

2. Если не REJECTED → Проверка на APPROVED
   ├─ coverage_percent >= 50%? ✓
   ├─ min_years_tested >= 2? ✓
   ├─ overall_score >= 4.0? ✓
   ├─ yield_score >= 4? ✓
   ├─ quality_score is not None AND >= 3? ✓
   └─ resistance_score is None OR >= 3? ✓
   → Если ВСЕ ✓ → APPROVED

3. Если не APPROVED → CONTINUE (по умолчанию)
   └─ Формируется список причин для продолжения
```

---

### 💡 Особенности логики

1. **Guard Clauses Pattern**: Использование защитных условий позволяет ранний выход из функции при обнаружении критических проблем

2. **Строгость проверки качества**: 
   - Качество **обязательно** должно быть не `None` для одобрения
   - Устойчивость может отсутствовать (`None`)

3. **Адаптивность**: 
   - Система формирует детальные обоснования для каждого решения
   - Предоставляет рекомендации по улучшению

4. **Уверенность (confidence)**:
   - `high` - для `rejected` и `approved` (четкие критерии)
   - `medium` - для `continue` (требуется дополнительный анализ)

5. **Зоны возделывания**:
   - `zones_recommended` формируется **только** для `approved` решений
   - `zones_with_positive_data` формируется всегда (для информации)

---

### 📝 Примеры реальных сценариев

#### Сценарий 1: Идеальный сорт
```
coverage_percent = 70%
min_years_tested = 3
overall_score = 4.5
yield_score = 5.0
quality_score = 4.0
resistance_score = 4.5

→ APPROVED ✅
```

#### Сценарий 2: Недостаточно данных
```
coverage_percent = 15%
min_years_tested = 1
overall_score = 3.0
yield_score = 3.0

→ REJECTED ❌ (низкое покрытие)
```

#### Сценарий 3: Хороший, но нужны дополнительные испытания
```
coverage_percent = 45%
min_years_tested = 1
overall_score = 4.2
yield_score = 4.5
quality_score = 3.5

→ CONTINUE ⏳ (недостаточно лет и покрытия)
```

#### Сценарий 4: Нет данных по качеству
```
coverage_percent = 60%
min_years_tested = 3
overall_score = 4.5
yield_score = 4.5
quality_score = None  # ❌
resistance_score = 4.0

→ CONTINUE ⏳ (отсутствуют данные по качеству)
```

---

### 🔗 Связь с другими компонентами

1. **BallScorer.get_decision_criteria()**: 
   - Предоставляет базовые критерии на основе баллов
   - Используется для предварительной оценки

2. **SummaryService._make_recommendation_new()**: 
   - Основная логика принятия решений
   - Учитывает не только баллы, но и метрики покрытия и продолжительности

3. **AnnualDecisionItem**: 
   - Модель для хранения решений
   - Связывает рекомендации с фактическими решениями комиссии

